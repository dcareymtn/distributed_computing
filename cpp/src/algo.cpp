#include <stdio.h>

#include "algo.h"
#include "matrix.h"
#include <vector>

std::vector<int> count_occurences( const Matrix & mat, int start_count, int stop_count )
{
    std::vector<int> counter;

    return counter;
}
